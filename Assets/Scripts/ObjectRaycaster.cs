using UnityEngine;

public class ObjectRaycaster : MonoBehaviour
{
    
//     PointerEventData pointerData = new PointerEventData(EventSystem.current);

//     pointerData.position = Input.mousePosition; // use the position from controller as start of raycast instead of mousePosition.

//     List<RaycastResult> results = new List<RaycastResult>();
//     graphicRaycaster.Raycast(pointerData, results);

//     if (results.Count > 0) {
//         //WorldUI is my layer name
//         // if (results[0].gameObject.layer == LayerMask.NameToLayer("UI")){ 
        
//         string dbg = "Root Element: {0} \n GrandChild Element: {1}";
        
//         Debug.Log(string.Format(dbg, results[results.Count-1].gameObject.name,results[0].gameObject.name));
         
//         results.Clear();
//         // }
//    }


// if (GraphicRaycaster.Raycast(ray, out RaycastHit hit, rayDistance))
// {
//     test.text = hit.transform.name;
//     if (hit.transform == gameObject.transform)
//     {
//         Debug.Log("Hit");
//     }
// }
// else
// {
//     test.text = "NULL";
// }

// Ray ray = new Ray(
//     origin: targetCamera.transform.position,
//     direction: targetCamera.transform.forward
// );


}
